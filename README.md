# TravelJournalDB

ระบบฐานข้อมูลสำหรับบันทึกการเดินทาง (Travel Journal Database)

---

## 📋 ภาพรวม

**TravelJournalDB** เป็นฐานข้อมูล SQL Server สำหรับจัดการบันทึกการเดินทาง ประกอบด้วย:

- สร้างฐานข้อมูล `TravelJournalDB`
- รันสคริปต์ **DDL** (ตาราง, ดัชนี, วิว, ฟังก์ชัน, สโตร์โพรซีเจอร์, ทริกเกอร์)
- รันสคริปต์ **DML** (ใส่ข้อมูลตัวอย่าง, ลิงก์ Entry ↔ Location, อัปเดตสถิติ, บันทึก Activity)

---

## 🚀 วิธีรัน (สำหรับรอบถัดไป/เครื่องอื่น)

### 1) ผ่าน SQL Server Management Studio (SSMS)
1. สร้างฐานข้อมูล ให้ทำการตั้ง “Database name : `TravelJournalDB` ดูข้อ2
2. ”ตั้งค่า “Collation:” เป็น “Thai_CI_AS” เพื่อให้รองรับอักขระภาษาไทย
3. เปิดไฟล์ `SQL_DDL_DML_DQL/DDL.sql` แล้วกด **Execute**
4. เมื่อสำเร็จ เปิดไฟล์ `SQL_DDL_DML_DQL/DML.sql` แล้วกด **Execute**

---

## 🗄️ โครงสร้างโดยสรุป (จาก DDL)

### Tables (ตาราง)
- `Users` - ข้อมูลผู้ใช้งาน
- `TravelEntries` - บันทึกการเดินทาง
- `Locations` - ข้อมูลสถานที่
- `EntryLocations` - ความสัมพันธ์ระหว่าง Entry และ Location
- `LocationStatistics` - สถิติการเยี่ยมชมสถานที่
- `ActivityLogs` - ประวัติกิจกรรมในระบบ
- `UserActivityLogs` - กิจกรรมของผู้ใช้แต่ละคน

### Views (วิว)
- `vw_PopularLocations` - สถานที่ยอดนิยม
- `vw_UserUniqueLocations` - จำนวนสถานที่ไม่ซ้ำที่ผู้ใช้เคยไป
- `vw_UserStatsSummary` - สรุปสถิติผู้ใช้งาน

### Function (ฟังก์ชัน)
- `fn_GetActivityCountByDateRange` - นับจำนวนกิจกรรมในช่วงเวลา

### Stored Procedures (สโตร์โพรซีเจอร์)
- `sp_RegisterUser` - ลงทะเบียนผู้ใช้ใหม่
- `sp_LoginUser` - เข้าสู่ระบบ
- `sp_AddTravelEntry` - เพิ่มบันทึกการเดินทาง
- `sp_UpdateLocationStatistics` - อัปเดตสถิติสถานที่
- `sp_LogUserActivity` - บันทึกกิจกรรมผู้ใช้
- ฯลฯ

### Trigger (ทริกเกอร์)
- `trg_UpdateStatsAfterEntry` - อัปเดตสถิติสถานที่แบบ set-based หลัง INSERT ที่ `EntryLocations`

---

## ✅ ตรวจสอบผลหลังรัน (DQL แนะนำ)

### รวมยอดข้อมูลทั้งหมด

```sql
USE TravelJournalDB;

SELECT 
  (SELECT COUNT(*) FROM dbo.Users)              AS TotalUsers,
  (SELECT COUNT(*) FROM dbo.TravelEntries)      AS TotalEntries,
  (SELECT COUNT(*) FROM dbo.Locations)          AS TotalLocations,
  (SELECT COUNT(*) FROM dbo.UserActivityLogs)   AS TotalUserActivities,
  (SELECT COUNT(*) FROM dbo.ActivityLogs)       AS TotalActivities;
```

### ดู Top สถานที่ยอดนิยม

```sql
SELECT TOP (10) 
  L.LocationName, 
  LS.VisitCount, 
  LS.AverageRating, 
  LS.PopularityScale
FROM dbo.LocationStatistics LS
JOIN dbo.Locations L ON L.LocationID = LS.LocationID
ORDER BY LS.VisitCount DESC, LS.AverageRating DESC;
```

### ดูสรุปผู้ใช้

```sql
SELECT * 
FROM dbo.vw_UserStatsSummary 
ORDER BY CreatedDate DESC;
```

---

## 🔄 การรีเซ็ต/รันซ้ำ

ถ้าจะรัน DML ซ้ำโดยไม่ชน UNIQUE constraint:

1. **รัน DDL ก่อน** (ที่มีส่วน DROP) เพื่อเคลียร์ออบเจ็กต์เก่า
2. **จากนั้นรัน DML** อีกครั้ง

```bash
# รีเซ็ตและสร้างใหม่
sqlcmd -S <SERVER_NAME> -E -i SQL_DDL_DML_DQL/DDL.sql
sqlcmd -S <SERVER_NAME> -E -i SQL_DDL_DML_DQL/DML.sql
```

---

## ⚙️ การปรับแต่ง

### ใช้เวลา UTC แทน Local Time

ต้องการเวลาเป็น UTC ให้ค้นหา/แทนที่ใน DDL/DML:

```sql
GETDATE()  →  GETUTCDATE()
```

---

## 👤 บัญชีทดสอบ (จาก DML)

| Username | Password |
|----------|----------|
| `admin` | `password123` |
| `john_doe` | `password123` |
| `jane_smith` | `password123` |

> ⚠️ **คำเตือน:** ใช้สำหรับการทดสอบเท่านั้น

---

## 📁 โครงสร้างไฟล์

```
TravelJournalDB/
└── SQL_DDL_DML_DQL/
    ├── DDL.sql          # สร้างโครงสร้างฐานข้อมูล
    └── DML.sql          # ใส่ข้อมูลตัวอย่าง
```
**Last Updated:** November 2025
